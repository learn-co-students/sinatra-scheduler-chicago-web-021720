<h1> Course Listings </h1>

<% @courses.each do |course| %>
    <h3>Course: <a href="/courses/<%= course.id %>"> <%= course.name %></a></h3>
    <h4>Proffessor: <%= course.professor %></h4>
    <h4>Credits: "<%= course.credits %>"</h4>
    <hr>
<% end %>